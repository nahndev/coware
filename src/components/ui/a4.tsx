import styled from "styled-components";

export type A4Props = {};

export const A4 = styled.div`
  width: 210mm;
  padding: 10mm;
  margin: 10mm auto;
  background: white;
  box-shadow: 0 0 5px rgba(0, 0, 0, 0.1);
`;

export default A4;
